# render.yaml — CodeAtlas ingestion service (backend/)
services:
  - type: web
    name: codeatlas-ingest-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /ping
